export declare const enum GeneralShellType {
    Bash = "bash",
    Csh = "csh",
    Fish = "fish",
    Julia = "julia",
    Ksh = "ksh",
    Node = "node",
    NuShell = "nu",
    PowerShell = "pwsh",
    Python = "python",
    Sh = "sh",
    Zsh = "zsh"
}
export declare const enum WindowsShellType {
    CommandPrompt = "cmd",
    GitBash = "gitbash",
    Wsl = "wsl"
}
export type ShellType = GeneralShellType | WindowsShellType;
export declare const windowShellTypesToRegex: Map<string, RegExp>;
export declare const shellTypesToRegex: Map<string, RegExp>;
/** Matches `user@host:` style prompts (e.g. `user@host:~/path`). */
export declare function looksLikeHostPrompt(value: string): boolean;
/** Checks whether a single token looks like a file-system path (starts with `/`, `~`, or `./`). */
export declare function looksLikePath(value: string): boolean;
/**
 * Cleans a shell-reported terminal title by stripping shell prefixes and
 * extracting the base command name. Returns an empty string for CWD/prompt-style
 * titles (e.g. `/home/user/project`, `user@host:~/path`) to signal that the
 * title should not be updated.
 */
export declare function cleanTerminalTitle(title: string): string;
export declare function guessShellTypeFromExecutable(executable: string | undefined): string | undefined;
//# sourceMappingURL=shell-type.d.ts.map