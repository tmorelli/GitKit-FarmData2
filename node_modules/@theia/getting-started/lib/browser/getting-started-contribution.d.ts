import { CommandRegistry, MenuModelRegistry, MessageService, PreferenceContribution, PreferenceSchemaService, PreferenceService, QuickInputService } from '@theia/core/lib/common';
import { AbstractViewContribution, FrontendApplicationContribution, FrontendApplication } from '@theia/core/lib/browser';
import { EditorManager } from '@theia/editor/lib/browser/editor-manager';
import { GettingStartedWidget } from './getting-started-widget';
import { FileService } from '@theia/filesystem/lib/browser/file-service';
import { FrontendApplicationStateService } from '@theia/core/lib/browser/frontend-application-state';
import { WorkspaceService } from '@theia/workspace/lib/browser';
import { ColorContribution } from '@theia/core/lib/browser/color-application-contribution';
import { ColorRegistry } from '@theia/core/lib/browser/color-registry';
import { WalkthroughService } from './walkthrough-service';
/**
 * Triggers opening the `GettingStartedWidget`.
 */
export declare const GettingStartedCommand: {
    id: string;
    label: string;
};
/**
 * How a walkthrough can be referenced when opening it through a command.
 * The object form is what VS Code's `workbench.action.openWalkthrough` accepts, e.g. from a `command:` link
 * in a step description.
 */
export type WalkthroughReference = string | {
    category?: string;
    step?: string;
};
export declare class GettingStartedContribution extends AbstractViewContribution<GettingStartedWidget> implements FrontendApplicationContribution, PreferenceContribution, ColorContribution {
    protected readonly commandRegistry: CommandRegistry;
    protected readonly editorManager: EditorManager;
    protected readonly fileService: FileService;
    protected readonly preferenceService: PreferenceService;
    protected readonly stateService: FrontendApplicationStateService;
    protected readonly workspaceService: WorkspaceService;
    protected readonly walkthroughService: WalkthroughService;
    protected readonly quickInputService: QuickInputService;
    protected readonly messageService: MessageService;
    constructor();
    initSchema(service: PreferenceSchemaService): Promise<void>;
    onStart(app: FrontendApplication): Promise<void>;
    protected openReadme(): Promise<void>;
    registerCommands(registry: CommandRegistry): void;
    /**
     * Open the welcome view and, if a walkthrough is given, select it.
     * Accepts both a plain id and the argument object that VS Code's `workbench.action.openWalkthrough` uses.
     * Without a walkthrough, the available ones are offered for selection.
     */
    protected openWalkthrough(walkthrough?: WalkthroughReference): Promise<void>;
    /**
     * Let the user choose one of the available walkthroughs, completed ones included.
     *
     * @returns the id of the chosen walkthrough, or `undefined` if there is nothing to choose or the user cancelled.
     */
    protected pickWalkthrough(): Promise<string | undefined>;
    registerMenus(menus: MenuModelRegistry): void;
    registerColors(colors: ColorRegistry): void;
}
//# sourceMappingURL=getting-started-contribution.d.ts.map