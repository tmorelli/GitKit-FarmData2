import { FrontendApplicationContribution } from '@theia/core/lib/browser';
import { MCPOAuthFrontendDelegate, MCPServerDescription, MCPServerManager } from '../common';
import { MCPFrontendNotificationService, MCPFrontendService } from '../common/mcp-server-manager';
import { MessageService, PreferenceService, Progress, ILogger } from '@theia/core';
import { WorkspaceTrustService, WorkspaceRestrictionContribution, WorkspaceRestriction } from '@theia/workspace/lib/browser/workspace-trust-service';
import { WorkspaceService } from '@theia/workspace/lib/browser/workspace-service';
import { MCPServersPreference } from '../common/mcp-server-preference-validator';
export declare class McpFrontendApplicationContribution implements FrontendApplicationContribution, WorkspaceRestrictionContribution {
    protected preferenceService: PreferenceService;
    protected manager: MCPServerManager;
    protected frontendMCPService: MCPFrontendService;
    protected workspaceTrustService: WorkspaceTrustService;
    protected readonly logger: ILogger;
    protected workspaceService: WorkspaceService;
    protected messageService: MessageService;
    protected oauthFrontendDelegate: MCPOAuthFrontendDelegate;
    protected mcpNotificationService: MCPFrontendNotificationService;
    protected prevServers: Map<string, MCPServerDescription>;
    protected blockedUntrustedServers: Set<string>;
    protected serverChangeQueue: Promise<void>;
    /** Pending OAuth sign-in prompts, keyed by server name. */
    protected readonly pendingSignInPrompts: Map<string, Progress>;
    onStart(): void;
    protected updateWorkspaceRoots(restart: boolean): Promise<void>;
    protected startPreviouslyBlockedServers(): Promise<void>;
    protected stopAllServers(): Promise<void>;
    protected updateBlockedServersStatusBar(): void;
    getRestrictions(): WorkspaceRestriction[];
    protected autoStartServers(servers: Map<string, MCPServerDescription>): Promise<void>;
    /**
     * Autostart path for a single server. For non-OAuth servers, just calls `startServer`. For OAuth
     * servers, either skips the silent start when no usable credentials exist (and prompts the user)
     * or attempts the silent start; if the silent start lands in `AuthenticationRequired`, prompts
     * the user via a notification with a Sign In action.
     */
    protected attemptSilentStartOrPromptForOAuth(name: string, serverDesc: MCPServerDescription): Promise<void>;
    /**
     * Prompts the user to authorize the given MCP server via a non-modal notification with a 'Sign In'
     * action. Dismissed automatically once the start attempt concludes, see {@link dismissSettledSignInPrompts}.
     */
    protected promptForOAuthSignIn(serverName: string): void;
    /** Dismisses sign-in prompts whose server was removed or whose start attempt has concluded. */
    protected dismissSettledSignInPrompts(): Promise<void>;
    protected enqueueServerChanges(newServers: MCPServersPreference): void;
    protected handleServerChanges(newServers: MCPServersPreference): Promise<void>;
    protected syncServers(servers: Map<string, MCPServerDescription>): Promise<void>;
    protected convertToMap(servers: MCPServersPreference): Map<string, MCPServerDescription>;
}
//# sourceMappingURL=mcp-frontend-application-contribution.d.ts.map