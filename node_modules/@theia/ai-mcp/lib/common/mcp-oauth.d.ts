export declare const MCP_OAUTH_CALLBACK_PATH = "/mcp/oauth/callback";
export interface MCPOAuthConfig {
    clientId?: string;
    /**
     * Optional client secret for authorization servers that require a pre-registered confidential
     * client. Only used together with {@link clientId}; ignored during dynamic client registration.
     */
    clientSecret?: string;
    scopes?: string[];
    authorizationServer?: string;
    resource?: string;
}
export interface MCPOAuthCallback {
    state: string;
    code?: string;
    error?: string;
    errorDescription?: string;
}
export declare const MCPOAuthFrontendDelegateClient: unique symbol;
export interface MCPOAuthFrontendDelegateClient {
    /**
     * Launches the authorization URL in the user's default browser.
     */
    openExternal(url: string): Promise<void>;
    getCallbackUrl(): Promise<string>;
}
export declare const MCPOAuthFrontendDelegate: unique symbol;
/**
 * Backend-side bridge to the browser client for OAuth operations that must run in the frontend.
 */
export interface MCPOAuthFrontendDelegate extends Pick<MCPOAuthFrontendDelegateClient, 'openExternal' | 'getCallbackUrl'> {
    setClient(client: MCPOAuthFrontendDelegateClient): void;
    disconnectClient(client: MCPOAuthFrontendDelegateClient): void;
    /**
     * The OAuth redirect URL actually advertised to authorization servers: the Electron loopback
     * callback URL when the loopback endpoint is bound, otherwise the frontend's origin-based
     * callback URL. Lets UI surfaces show users which redirect URI to register with a provider.
     */
    getEffectiveRedirectUrl(): Promise<string>;
}
export declare const mcpOAuthFrontendDelegatePath = "/services/mcpOAuthFrontendDelegate";
//# sourceMappingURL=mcp-oauth.d.ts.map