export interface PackageTranslation {
    translation?: Record<string, string>;
    default?: Record<string, string>;
}
interface LocalizeInfo {
    message: string;
    comment?: string;
}
export declare function coerceLocalizations(translations: Record<string, string | LocalizeInfo>): Record<string, string>;
/**
 * Recursively walks `value` and replaces strings matching `%key%` with the result of `resolve(key)`.
 * If `resolve(key)` returns `undefined` or an empty string, the original `%key%` is kept
 * (same as the former worker `localize`: `translations[key] || value`).
 */
export declare function localizeWithResolver<T>(value: T, resolve: (key: string) => string | undefined): T;
export type LocalizePackageCallback = (key: string, defaultValue: string) => string;
/**
 * Recursively replaces %key% strings in value with translations.
 * callback is used when only default (package.nls.json) exists; e.g. backend passes
 * Theia's Localization.localize for fallback, build-time can use (_, d) => d.
 */
export declare function localizePackage<T>(value: T, translations: PackageTranslation, callback: LocalizePackageCallback): T;
export {};
//# sourceMappingURL=package-nls.d.ts.map